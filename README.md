# mcp-sandbox-setup
Package that uses a Docker compose stack to set up a containers for a remote MCP server and reverse proxy on a remote host. Ideal for blast radius containment for LLM/code testing.
